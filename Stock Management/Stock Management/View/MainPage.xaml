<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stock_Management"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Viewmodel="using:Stock_Management.Viewmodel"
    x:Class="Stock_Management.MainPage"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="ProductTemplate">
            <Grid/>
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate1">
            <Grid/>
        </DataTemplate>
        <DataTemplate x:Key="ProductTemplate1">
            <Grid Height="215" Width="184">
                <StackPanel HorizontalAlignment="Left" Height="171" Margin="10,34,0,0" VerticalAlignment="Top" Width="164">
                    <TextBlock TextWrapping="Wrap" Text="{Binding Name}"/>
                    <TextBlock TextWrapping="Wrap" Text="{Binding Description}"/>
                    <TextBlock TextWrapping="Wrap" Text="{Binding Price}"/>
                    <TextBlock TextWrapping="Wrap" Text="{Binding Stock}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Page.DataContext>
        <Viewmodel:ProductViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView HorizontalAlignment="Left" Height="390" Margin="548,100,0,0" VerticalAlignment="Top" Width="282" CanReorderItems="True" ItemsSource="{Binding ProductCatalogSingleton.ProductList}" SelectionChanged="ListView_SelectionChanged">
            <TextBlock TextWrapping="Wrap" Text="TextBlock"/>
        </ListView>
        <ListView HorizontalAlignment="Left" Height="161" Margin="286,266,0,0" VerticalAlignment="Top" Width="257"/>

    </Grid>
</Page>
